
sudo apt update
sudo apt install python3 python3-pip python3-venv
python --version
pip --version
----------
cp,mv,mkdir,ls,cat,tail
----------
Create venv
A virtual environment (venv) is an isolated Python environment for a specific project.

source venv/bin/activate
pip install requests
pip freeze > requirements.txt
------------
Ollama
ollama pull <model-name>    pull - downlaod
ollama run phi3             run
ollama show phi3            show info
ollama rm <model-name>      remove
------------
Docker
docker ps

nano Dockerfile
(add this)
    FROM ubuntu:latest
    ENV DEBIAN_FRONTEND=noninteractive
    RUN apt update && apt install -y curl vim
    CMD ["bash"]

Create image
docker build -t my-image-name .

Run Container of image
docker run my-image-name
docker run -d myapp (for bg)
docker images
sudo systemctl status docker

enter in Container
docker exec -it mycontainer bash

Delete Container
docker stop mycontainer
docker rm mycontainer

Delete image
docker rmi myapp

docker system prune -f